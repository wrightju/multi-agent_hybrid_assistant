# config/constants.py

# Constants and shared settings for the project

# Example constants
DEFAULT_SECURITY_LEVEL = 'public'
DEFAULT_LOGGING_LEVEL = 'INFO'

# Paths
SHARED_REFERENCE_MATERIALS_PATH = 'shared/reference_materials/'
SHARED_VECTOR_STORE_PATH = 'shared/vector_store/'

# API Endpoints (example if needed for any external integrations)
EXTERNAL_API_ENDPOINT = 'https://api.example.com/endpoint'

# Timeout values (if needed for any operations)
DEFAULT_TIMEOUT = 30  # in seconds

# Add any additional constants as needed, such as default settings for agents, etc.

# Usage example:
# from config.constants import DEFAULT_SECURITY_LEVEL, SHARED_REFERENCE_MATERIALS_PATH
